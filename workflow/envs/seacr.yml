name: chipseq-seacr
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  # SEACR brings an R runtime; keep it out of the core environment because
  # SEACR sidecar peaks are optional and disabled by default.
  - seacr >=1.3,<2
  - bedtools >=2.31,<3
