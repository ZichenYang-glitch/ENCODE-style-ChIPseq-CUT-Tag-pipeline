# v0.1.0-beta Release Verification

**Date:** 2026-05-19
**Time:** 04:22 UTC
**Checklist reference:** README > Developer Notes > Release checklist
**Overall result:** 6/6 local checks PASS, CI pending manual check

## Local checks

### 1. Validate default config and stress-test validation

- **`python3 scripts/validate_samples.py --config config/config.yaml`**
  - Result: PASS
  - Exit code: 0
  - Notes: "OK: 1 sample(s) validated (1 treatment, 0 control)"

- **`python3 test/test_validation_stress.py`**
  - Result: PASS
  - Exit code: 0
  - Notes: 15/15 tests passed

### 2. Default DAG check

- **`snakemake -s workflow/Snakefile --configfile config/config.yaml -n --quiet`**
  - Result: PASS
  - Exit code: 0
  - Notes: The command passed when executed through the `chipseq` Conda environment (`conda run -n chipseq snakemake -s workflow/Snakefile --configfile config/config.yaml -n --quiet`). The current shell did not have `snakemake` on PATH, so `conda run` was used to run the same dry-run inside the release environment.

### 3. Dry-run smoke profiles

- **`python3 test/test_stage8_smoke_profiles.py`**
  - Result: PASS
  - Exit code: 0
  - Notes: 7/7 profiles passed (chipseq_se_noctrl, chipseq_pe_noctrl, chipseq_pe_ctrlsample, cuttag_pe_noctrl, cuttag_pe_seacr, chipseq_idr_dryrun, chipseq_pe_external_ctrlbam). Note: smoke profiles invoke snakemake internally; the harness resolves snakemake at runtime.

### 4. Tiny real execution

- **`python3 test/test_stage8b_tiny_execution.py`**
  - Result: PASS
  - Exit code: 0
  - Notes: "PASS: Stage 8b tiny real execution — Pipeline ran successfully through preprocessing + signal on tiny synthetic data. Verified: FastQC, Trim Galore, Bowtie2, sort, index, filter, dedup, final.bam, BigWig." All output under `/tmp`.

### 5. Repo hygiene

- **`git status --short --untracked-files=all`**
  - Result: PASS
  - Exit code: 0
  - Notes: No `results/`, `.snakemake/`, `*.fq`, `*.fq.gz`, `*.bam`, `*.bai`, `*.bw`. Only untracked items are `docs/superpowers/specs/2026-05-19-stage10c-release-checklist-design.md` and `docs/release-checks/` (this check's own artifacts).

### 6. CI status

- **GitHub Actions fast-checks on main:**
  - Status: Pending manual check
  - Notes: Latest CI run #22 (commit 229ee7d, "Add focused user documentation", branch main, ~22s). Conclusion could not be determined from CLI (`gh` not available). Check the Actions tab at https://github.com/ZichenYang-glitch/ENCODE-style-ChIPseq-CUT-Tag-pipeline/actions for the latest green/red status.

- **Manual real-execution (workflow_dispatch):**
  - Status: Not run for v0.1.0-beta local verification; manual workflow_dispatch remains optional.

## Results summary

| # | Check | Result | Exit code | Notes |
|---|-------|--------|-----------|-------|
| 1a | validate_samples config | PASS | 0 | 1 treatment, 0 control |
| 1b | validation_stress | PASS | 0 | 15/15 tests passed |
| 2 | DAG check | PASS | 0 | passed via `conda run -n chipseq` |
| 3 | smoke profiles | PASS | 0 | 7/7 profiles passed |
| 4 | tiny execution | PASS | 0 | preprocessing + signal on synthetic data |
| 5 | repo hygiene | PASS | 0 | no generated artifacts |
| 6a | CI fast-checks | Pending | — | latest run #22; check GitHub UI |
| 6b | CI real-execution | Not run | — | optional manual dispatch |
